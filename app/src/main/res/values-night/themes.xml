<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Trab1ProgMov" parent="Theme.MaterialComponents.DayNight.DarkActionBar.Bridge">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/corFundoNoite</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/corFundoNoite</item>
        <item name="colorSecondaryVariant">@color/teal_200</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="actionBarItemBackground">?attr/colorPrimary</item>
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <item name="android:windowBackground">@color/corFundoNoite</item>
        <item name="android:navigationBarColor">@color/corNavBarNoite</item>
        <!-- Nav bar -->
        <item name="bottomNavigationStyle">@style/Widget.App.BottomNavigationView</item>
    </style>

    <style name="Widget.App.BottomNavigationView" parent="Widget.MaterialComponents.BottomNavigationView.Colored">
        <item name="materialThemeOverlay">@style/ThemeOverlay.App.BottomNavigationView</item>
    </style>

    <style name="ThemeOverlay.App.BottomNavigationView" parent="">
        <item name="colorPrimary">@color/corNavBarNoite</item>
        <item name="colorOnPrimary">@color/black</item>
    </style>

    <!-- Button -->
    <style name="ButtonStyle" parent="@android:style/Widget.Button">
        <item name="android:background">@color/corFundoNoite</item>
        <item name="android:textColor">@color/white</item>
    </style>

    <style name="Theme.Uva" parent="Theme.MaterialComponents.DayNight.DarkActionBar.Bridge">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/uva</item>
        <item name="colorPrimaryVariant">@color/uva</item>
        <item name="colorOnPrimary">@color/uva</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/uva</item>
        <item name="colorSecondaryVariant">@color/uva</item>
        <item name="colorOnSecondary">@color/uva</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimary</item>
        <item name="android:windowBackground">@color/uva</item>
        <item name="android:textColor">@color/uva</item>
        <item name="android:navigationBarColor">@color/uva</item>
        <!-- Nav bar -->
        <item name="bottomNavigationStyle">@style/Widget.App.BottomNavigationView</item>
    </style>
</resources>